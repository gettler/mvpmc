#!/usr/bin/python
#
# SConscript
#

import os

Import ('env')

mvp = env.Copy()
libdir = mvp['INSTLIBDIR']
mysql_libdir = mvp['INSTLIBDIR'] + '/mysql'
target = mvp['TARG']
toplevel = mvp['TOPLEVEL']
bindir = mvp['INSTBINDIR']
linkmode = mvp['LINKMODE']
gtkcflags = env['GTKCFLAGS']
gtkldflags = env['GTKLDFLAGS']
ldflags = env['LDFLAGS']

#
# XXX: dslinux uclibc build does not include large file support...
#
if target != 'mg35':
	mvp.Append(CCFLAGS = ' -D_FILE_OFFSET_BITS=64 -D_LARGEFILE_SOURCE -D_LARGEFILE64_SOURCE')
	env.Append(CCFLAGS = ' ' + gtkcflags)

version = os.system(toplevel + '/src/version.sh ' + toplevel + ' ' + toplevel + '/src')

if target == 'host':
	libx11dir = ['/usr/X11R6/lib']
	libFLAC = []
	libx11 = ['X11', 'pthread', 'rt', 'Xau', 'dl', 'Xdmcp']
	libs = []
	linkflags = ''
	mvpapps = []
else:
	libx11dir = []
	libFLAC = ['libFLAC']
	libx11 = []
	libs = [ 'tiwlan' ]
	linkflags = ''
	mvpapps = ['ticonfig.c', 'vpdread.c']

if target == 'mvp':
	splash = mvp.Program('splash',
                             ['splash.c'],
                             CPPPATH = [ env['INCDIR'], env['INSTINCDIR'],
                                         '.', '../include' ],
                             LIBS = ['osd', 'av', 'dl'],
			     CCFLAGS = env['CCFLAGS'] + ' -DSTANDALONE',
                             LIBPATH = [ libdir ],
                             LINKFLAGS = [ '' ])

common = [ 'filebrowser.c', 'mvpmc.c', 'plugin.c', 'version.c' ]

if target == 'mvp':
	src = [ common ]
	pt = 'pthread'
elif target == 'mg35':
	src = [ common ]
	pt = 'pthread_nommu'
else:
	src = [ common ]
	pt = 'pthread'

if linkmode == 'static':
	plugins = [ 'mvpmc_html.plugin', 'mvpmc_osd.plugin' ]
	libs = [ 'gw', 'refmem', pt ]
	linkflags = '-static'
else:
	plugins = [ ]
	libs = [ 'dl', 'gw', 'refmem', pt ]
	linkflags = ''

mvpmc = mvp.Program('mvpmc', src,
                    CPPPATH = [ env['INCDIR'], env['INSTINCDIR'],
                                '.', '../include' ],
                    LIBS = plugins + libs ,
                    LIBPATH = [ libdir ],
                    LINKFLAGS = ldflags + ' ' + linkflags + ' ' + gtkldflags)

if target == 'mvp':
	inst = env.Install(bindir, [ mvpmc, splash ])
else:
	inst = env.Install(bindir, mvpmc)

env.Depends(mvpmc, version)
env.Depends(inst, mvpmc)

ret = [ mvpmc, inst ]

Return('ret')
